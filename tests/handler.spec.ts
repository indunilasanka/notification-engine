import {expect} from 'chai';

describe('Calculator', () => {
    describe('Add', () => {
        it('Should return 3 when a = 1 and b = 2', () => {
            expect(3).to.equal(3);
        });
    });
});
